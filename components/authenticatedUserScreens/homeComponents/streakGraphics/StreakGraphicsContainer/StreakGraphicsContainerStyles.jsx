import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  streakContainer: {
    display: 'flex',
    flex: 1,
    flexDirection: 'row',
    flexWrap: 'nowrap',
    height: 10,
  }
});